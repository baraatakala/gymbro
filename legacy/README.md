# Legacy v1 (archived)

This folder holds the **original single-file** Gym Progress Tracker (`index.html`, ~7k lines) that was deployed to GitHub Pages in 2025.

The live site at [baraatakala.github.io/gymbro](https://baraatakala.github.io/gymbro/) now serves **GymBro 2.0** (React + Vite + Supabase) from the repository root.

Old root-level files (`index.html`, `quote1.jpg`–`quote4.jpg`) were removed from `main` so Pages no longer serves them. Motivation images for v2 live in `public/quotes/`.
